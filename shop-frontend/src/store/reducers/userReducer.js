const initialState = {
    user: '',
    id: '',
    isAdmin: false,
    isAuthenticated: true
};
const userReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
};

export default userReducer
